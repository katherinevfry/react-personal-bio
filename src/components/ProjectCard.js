import React from 'react';
import {
  Card
} from 'reactstrap';
import PropTypes from 'prop-types';

export default function projectCard({...player}) {
  return (
    <div>
      <Card>

      </Card>
    </div>
  );
}

project
